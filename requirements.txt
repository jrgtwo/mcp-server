# Core server framework
fastmcp

# HTTP client (weather tool, fetch_url)
httpx

# HTML parsing (fetch_url)
beautifulsoup4

# PDF text extraction (read_pdf)
pypdf

# ML — install the CUDA variant manually if needed:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch
transformers

# Model loading optimisations (recommended for large models)
accelerate

# HTTP transport (starlette CORS middleware)
starlette

# Stock price data (get_stock_price tool)
yfinance

# Audio transcription (transcribe_audio tool) — local Whisper inference
faster-whisper
